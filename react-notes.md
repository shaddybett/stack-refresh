React revolves around componenets
package.json contains all dependencies for our application including the react framework
on the start of the project we ran npm instal to install the dependencies in package.json to our machine. Once installed they are place in the node_modules directory 